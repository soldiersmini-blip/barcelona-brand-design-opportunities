# Round 31：高分直连复核与完整机会总账

核验日期：2026-08-12

本轮不扩充虚假数量，先处理当前主表最高分岗位，并把所有既有搜索记录导出为可检查的完整总账。判断优先级仍是：具体职位原始页 / 官方 ATS > 雇主职位总表 > LinkedIn 雇主原帖 > 搜索缓存与聚合页。

## 本轮结论

- 当前主表：242 个独立机会。
- 地点：184 个 Barcelona / 周边，58 个明确 Spain / Europe / worldwide remote 或已标出 Spain 合同待确认的远程机会。
- 状态：219 个原始页显示可申请，23 个需要先确认；关闭岗位不进入主表。
- 中文相关：6 个有证据支持的中文、华人圈、中国公司或中文远程相关机会，没有用旧帖或非设计岗补数。
- 完整去重来源库：1,246 条，全部保留并导出，未因默认视图隐藏而删除。

## 前 25 名逐条直连结果

已逐一请求前 25 名的具体职位页。Dragons Junior、LABHOUSE、Trivelta、INFiLED、devicenow、Savills、Xapo、THE/STUDIO、Brownie、Textura、Kraken、Lodgify、Canonical、Fantasy、Bakken & Bæck、Supabase、reboot、Frekuent、Dragons Healthcare、Exoticca、CrowdStrike、Velvet Caviar 与 Hungry Minds 仍能对应到具体职位或当前申请路由。

三项需要回写：

1. CATORCE Visual Designer（ID 930839）
   - 旧 Greenhouse requisition `4797510008` 直连会跳到当前职位板并带 `error=true`。
   - 当前职位板已不再列出 Visual Designer；搜索缓存仍显示旧申请表，不能作为开放证据。
   - 旧卡移入关闭历史。当前仍开放的 `Studio Designer & HTML Programmer` 是另一职位，ID 1303 已独立保留在主表后部。

2. THRU Graphic Designer with Motion Skills（ID 1107）
   - 正文仍列出 Barcelona 现场为主、周五部分远程、全职、2026-09-15 截止、动效测试与邮件投递。
   - 同一页面页脚却写有 `NO JOB OPENINGS`。
   - 由“可投”降为“先确认”；先邮件询问是否仍收申请，再决定是否完成耗时的 After Effects 复刻测试。

3. Hungry Minds（ID 1310）
   - 修复乱码/空标题为 `Art Director - Creative Department (eCommerce)`。
   - 原 LinkedIn 职位仍返回 Spain 与申请入口，但完整 JD、薪资、合同、语言和资历未公开。
   - 保留为较低置信度 Spain-remote 电商艺术指导机会，不允许其冒充信息完整的品牌设计岗。

## 搜索结果的去留

- Appspace Visual Designer：官方 Greenhouse 详情跳到 `error=true`，当前职位板 13 个岗位中没有设计岗；继续保持关闭历史。
- MiiN / The Skin Lounge Graphic Designer：官方页面明确 `Closed job listing`；不因搜索结果的新鲜时间恢复。
- 10x Team Visual / Graphic AI Trainer：当前 Spain 旧编号已从官方板移除或属于非固定项目池；不计为当前正式设计 vacancy。
- AtomiQ Lead Visual Designer：Remote Spain、€50k–60k、Apply Online 仍在；已经是主表现有卡，不重复添加。
- Fail Fast Visual Designer：Barcelona hybrid、+5/8 年资历与西语门槛明确；已经是主表现有卡，不重复添加。
- Nacar Visual Designer (Banners)：只有第三方近期镜像，官方 Recruitee 当前未列该岗；不进入主表。
- Social Scout：雇主文本写全球 contractor，但地点与 Spain 合同证据冲突；继续留在外地/资格不明区。
- 西华论坛旧“广告店全职设计师”来自 2026-01-15，不是本轮新岗位，继续保留历史，不以旧帖增加中文当前数量。

## 总账导出

- `current-opportunity-ledger.csv`：242 个当前主机会，严格按网站分数从高到低。
- `current-opportunity-ledger.json`：同一主表的结构化 JSON。
- `all-opportunity-ledger.csv`：1,246 条去重来源记录，带 `current-main`、`review-required`、`madrid-excluded`、`other-location-or-unclear`、`closed-history` 等归属。

每行包含公司、中文岗位名、原岗位名、状态、地点、语言路径、方向、中文相关性、分数、发布日期、原始链接、核验证据和下一步分析。

## 排序与计数规则

- 只有 `MY_OPPORTUNITY_IDS` 中的 242 个独立记录进入默认主表。
- 主表显示分数是唯一的 100–1 序列，所有卡片严格降序；关闭的 CATORCE 被移除后重新生成序列。
- Madrid、Manresa 高通勤、地区资格不明、非设计、重复镜像和关闭记录不会混入默认主表，但完整历史仍在导出与来源库中。
- 页面动态计数与可见卡片来自同一主表，不再用来源总数冒充当前可投数量。
